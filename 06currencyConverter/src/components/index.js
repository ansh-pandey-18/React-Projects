import InputBox from "./InputBox.jsx"

export {InputBox}

/*
index.js file in components allows us to export all components from same file 
instead of exporting from every component from different files
*/